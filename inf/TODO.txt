1. Злочинні та мафіозні угруповання (дані про подільників).
    1.1. Make a page with a list of bands.
    1.2. When click on a band - open its members.
    1.3. Make a form to add new member to some band ("Add" button behind name of the band).
    1.4. Make a form (field) to search for a band by its name.

2. Вибірка за будь-якими ознаками, або навіть за підмножиною ознак.
(done)

3. Перенесення тих що «зав’язали» в архів; видалення - тільки після смерті.
    3.1. Make an archive page with a list of archived criminals
    3.2. Add on the main page a button "Add to Archive" for each criminal in the table.

4. Form to add new criminal and criminal details.
(done)

5. Form to update a criminal and criminal details (same page as See criminal details, just add input fields and a submit button).

            <p class="card-text fs-4"><strong>Criminal Details:</strong>
                <span th:if="${criminal.criminalDetails.size() > 0}">
                    <ul>
                        <li th:each="detail : ${criminal.criminalDetails}">
                            <label th:for="${'criminalDetails'+detail.attributeType}" th:text="${detail.attributeType}"></label>
                            <input type="text" class="form-control"
                                   th:if="${detail.attributeType.toString() != 'BIRTH_DATE'}"
                                   th:id="${'criminalDetails'+detail.attributeType}" name="criminalDetails"
                                   th:value="${detail.attributeValue}">
                            <input type="text" class="form-control"
                                   th:if="${detail.attributeType.toString() == 'BIRTH_DATE'}"
                                   th:id="${'criminalDetails'+detail.attributeType}" name="criminalDetails"
                                   th:value="${detail.attributeValue}">
                            <br>
                            <li th:each="type : ${detailsTypes}">
                                <input type="text" th:if="type == detail.attributeType" th:value="${detail.attributeValue}">
                                <input type="text" th:if="type != detail.attributeType" th:value="${detail.attributeValue}">
                            </li>
                        </li>
                    </ul>
                </span>
                <span th:unless="${criminal.criminalDetails.size() > 0}">
                    <input type="text" class="form-control" name="criminalDetails" placeholder="Add new criminal detail">
                </span>
            </p>




                    <li th:each="detailType : ${detailsTypes}">
                        <label th:for="${detailType}" th:text="${detailType}"></label>
                        <input type="text" class="form-control"
                               th:id="${detailType}"
                               th:value="${detailsMap.get(detailType)}"
                               th:name="${detailsMap}" >
                        <!--                               name="detailsMap['${detailType}']"-->
                        <br>
                    </li>